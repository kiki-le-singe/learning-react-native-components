import { AppRegistry } from 'react-native';
import ReactNativeComponents from './src/ReactNativeComponents';

AppRegistry.registerComponent('LearningReactNativeComponents', () => ReactNativeComponents);
